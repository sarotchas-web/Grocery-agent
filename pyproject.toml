[project]
name = "grocery-agent"
version = "0.1.0"
description = "Household grocery comparison rules for delivery and pickup."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
